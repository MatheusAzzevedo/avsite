# OS
.DS_Store
Thumbs.db

# Dependencies
node_modules/

# Environment
.env
.env.local
.env.production
 
dev.Dockerfile
docker-compose.yml
.agent/

# Editor
# O .sublime-workspace guarda estado local (arquivos abertos, histórico de
# busca) e nunca deve ser versionado; o .sublime-project acompanha, por ser
# gerado junto e específico de quem edita.
*.sublime-workspace
*.sublime-project
